import { WithRedirectButtonDirective } from './with-redirect-button.directive';

describe('WithRedirectButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new WithRedirectButtonDirective();
    expect(directive).toBeTruthy();
  });
});
