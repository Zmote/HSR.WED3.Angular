import { WithFilteringDirective } from './with-filtering.directive';

describe('WithFilteringDirective', () => {
  it('should create an instance', () => {
    const directive = new WithFilteringDirective();
    expect(directive).toBeTruthy();
  });
});
